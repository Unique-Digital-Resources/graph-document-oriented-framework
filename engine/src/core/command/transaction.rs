//! Transaction logic for grouping commands.
//! 
//! (Implementation lives in history::macro, re-exported here to match 
//! the architectural file layout from the roadmap).

pub use crate::core::history::r#macro::{CommandStep, Transaction};